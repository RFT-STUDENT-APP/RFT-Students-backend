meta {
  name: Apply
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admissions/apply
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "schoolId": "{{schoolId}}",
    "departmentId": "DEPT_ID"
  }
}
