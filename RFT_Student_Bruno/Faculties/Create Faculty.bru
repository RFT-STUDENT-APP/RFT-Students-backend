meta {
  name: Create Faculty
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/faculties
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Engineering",
    "schoolId": "{{schoolId}}"
  }
}
