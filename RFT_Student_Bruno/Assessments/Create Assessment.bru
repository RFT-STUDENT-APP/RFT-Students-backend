meta {
  name: Create Assessment
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/assessments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "Midterm Quiz",
    "type": "quiz",
    "courseId": "{{courseId}}"
  }
}
